## What

<!-- One paragraph: what changes and why. Link the issue or spec. -->

Closes #

## Tier

- [ ] Tier A (a path the repository's `CLAUDE.md` lists: auth, redirect or download targets, schema, packages, release workflows) — `reviewer-a` reviews, the orchestrator reads the hunks
- [ ] Tier B

## Verification

<!-- Commands run and their exit codes. A screenshot or measurement for UI. -->

## Checklist

- [ ] No secrets, credentials, names or emails in the diff
- [ ] The repository's own check wrapper passes locally (`npm run check` where it exists)
- [ ] File maps and docs updated where a file was added, removed or its behaviour changed
- [ ] Deferred review remarks filed as `follow-up` issues, not left in the thread
